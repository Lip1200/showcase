import React from "react";
import CVContent from "../sub/CVContent";

const Hero = () => {
  return (
    <div className="relative flex flex-col h-full w-full" id="cv">
      <CVContent />
    </div>
  );
};

export default Hero;
